# 是否启用统一统计（拦截所有 LLM 调用，包括插件内部调用）
# true: 统计所有调用（主对话+内部），并按规则分类
# false: 仅统计主对话（原有行为）
# 注意：此功能目前为预留功能，暂未实现
use_unified_stats: true

# Token统计插件配置示例
# 规则按列表顺序依次匹配，一旦匹配成功即停止后续匹配
# 每条规则的作用已用中文说明，您可以根据需要修改或添加新规则

plugins:
 